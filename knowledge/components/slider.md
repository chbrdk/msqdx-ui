# Slider

Atom in `@msqdx/ui` — tokenized `input[type=range]` with fill track via `--ds-slider-pct`.

Spec: `specs/domain/msqdx-ui-slider.md`

## Usage

Prefer `Meter` / `MeterList` when showing label + percent readout (persona traits, journey dimensions).

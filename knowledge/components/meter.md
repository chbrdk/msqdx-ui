# Meter / MeterList

Molecule wrapping `Slider` with magazine meter head (label · hint · value).

Spec: `specs/domain/msqdx-ui-meter.md`

## Migration

AUDION: replace `.audion-magazine-meters` + `.audion-editable-traits-slider` with `MeterList` / `Meter` / `Slider` from `@msqdx/ui`.

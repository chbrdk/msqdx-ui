# MarkerCanvas

Shared evidence / region overlay frame extracted from Brandion Findings + Detection Lab patterns.

## Follow-up

- Brandion `findings-document-canvas` keeps PDF/fixture paints; wrap the painted bitmap as `media` and map `EvidenceAnchor.bbox` → normalized `MarkerCanvasRect`.
- Detection Lab region boxes can use the same tone tokens (`pass` / `fail` / `warn`).

## Storybook

`Organisms/MarkerCanvas` — MediaSlot / Interactive / Empty.

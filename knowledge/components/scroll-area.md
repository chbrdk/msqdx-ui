# ScrollArea

Opt-in hairline scrollport (`.ds-scroll` + overflow orientation).

Spec: `specs/domain/msqdx-ui-scroll-area.md`

Never style `*::-webkit-scrollbar` globally — that forces classic gutters on composers and every overflow:auto surface.

# TokenPicker browser list density

**Date:** 2026-09-11  
**Spec:** `specs/domain/msqdx-ui-token-picker.md`

## Locked density

- Shared option min-height `2.125rem`, padding `0.5rem 0.65rem` (color + type + length).
- Search + “Type to search all N options” / empty: `0.625rem`.
- Font/type rows: `option--lead` — label flush left (no empty value column).
- Color without `valueLabel`: `option--swatch` (swatch + name).
- Length / labeled color: `option--columns` (preview · value · name).

import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './Icons.stories'

<Meta of={Stories} />

# Icons

Layer: `Foundation`

Shared Lucide wrappers used by nav rails and chat chrome. Import named `Icon*` exports from `@msqdx/ui`.

## Gallery

<Canvas of={Stories.Gallery} />

import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './Icons.stories'

<Meta of={Stories} />

# Icons

Layer: `Foundation`

Named `Icon*` exports from `@msqdx/ui`. Waves 1–3 are custom SVGs (`ds-ui-icon`). Residual Lucide wraps are the Align/Justify matrix only (Wave 4).

Spec: `specs/domain/msqdx-ui-icon-language.md` · Inventory: `knowledge/icon-catalog.md`

## Custom sample (Waves 1–2)

<Canvas of={Stories.CustomLanguage} />

## Custom language (Wave 3)

Media, nav/org, layout props, effects.

<Canvas of={Stories.CustomWave3} />

## Custom large (48px)

<Canvas of={Stories.CustomLarge} />

## Size ladder

<Canvas of={Stories.SizeLadder} />

## Residual Lucide (Align matrix)

<Canvas of={Stories.Gallery} />

import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './Icons.stories'

<Meta of={Stories} />

# Icons

Layer: `Foundation`

Named `Icon*` exports from `@msqdx/ui`. Waves 1–2 are custom SVGs in the shared stroke language with inspect glyphs (`ds-ui-icon`). Residual exports remain Lucide wraps until Vision D completes.

Spec: `specs/domain/msqdx-ui-icon-language.md` · Inventory: `knowledge/icon-catalog.md`

## Custom language (Wave 1)

<Canvas of={Stories.CustomLanguage} />

## Custom language (Wave 2)

Typography toolbar, editor chrome, status.

<Canvas of={Stories.CustomWave2} />

## Custom large (48px)

Hairline strokes stay sharp at docs scale.

<Canvas of={Stories.CustomLarge} />

## Size ladder

<Canvas of={Stories.SizeLadder} />

## Residual Lucide wraps

Still Lucide until later migration waves (media, nav, align matrix, …).

<Canvas of={Stories.Gallery} />

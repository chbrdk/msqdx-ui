import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './KpiStrip.stories'

<Meta of={Stories} />

# KpiStrip

Layer: `Organisms`

Horizontal KPI cards for overview headlines. Optional `onClick` turns a card into a button.

## Usage

<Canvas of={Stories.Default} />

## Related

Spec: `specs/domain/msqdx-ui-overview-surfaces.md`

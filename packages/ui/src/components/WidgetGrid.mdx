# WidgetGrid

Layer: `organisms`

Constrained dashboard widget layout (CSS grid). Spec: `specs/domain/msqdx-ui-widget-grid.md`.

## Joined board

`joined` forces gap none and applies hairline chrome on nested `.ds-panel--card` children. Set `data-row-end` / `data-last-row` from `layoutJoinedBoardEdges`.

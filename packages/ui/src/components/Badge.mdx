# Badge

Layer: `atoms`

Scaffolded via `pnpm ds:add Badge --layer atoms`.

## Next steps

- Align API with `specs/domain/msqdx-ui-badge.md`
- Replace the scaffold markup with the final primitive implementation
- Add meaningful stories and assertions

## Tokens consumed

DS-DEPOSIT D5 — tones consume Brandion paths via Layer 0 CSS. Enums stay React API.

See `knowledge/ds-deposit-component-semantic-map.md`.

| Tone | CSS | Brandion path |
|------|-----|---------------|
| accent | `--accent` | `color.action.primary` |
| success | `--success` (= `--ok`) | `color.status.ok` |
| warning | `--warning` (= `--warn`) | `color.status.warn` |
| danger | `--danger` | `color.status.danger` |


# ChatKeyValueList

Layer: `molecules`

Label → value rows for assistant detail blocks. Spec: `specs/domain/msqdx-ui-chat-key-value-list.md`.

Compose inside `ChatBlockPanel`. Full tour: **Organisms/ChatCatalog**.

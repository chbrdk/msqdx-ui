# InspectDock

Molecule — band inside a chat/message stream for agent inspect chrome.

Spec: `specs/domain/msqdx-ui-inspect-dock.md`

Compose with `StepStrip` and `EventFooter`. Domain mapping stays in the product.

# ContextMenu

Layer: `molecules`

Controlled pointer-positioned action menu for right-click authoring.

Spec: `specs/domain/msqdx-ui-context-menu.md`

## Usage

Pass `open`, screen coordinates `x`/`y`, `items`, and `onClose`. Apps own handlers; chrome stays in `@msqdx/ui`.

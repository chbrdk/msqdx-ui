import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './InspectLayoutGlyphs.stories'

<Meta of={Stories} />

# Inspect Layout Glyphs

Layer: `Foundation`

Theme-aware SVG diagrams for inspect chrome: size modes (Hug / Fill / Fixed), media fit (Cover / Contain / …), and grid column / span presets.

Fills use `--ink` (readable on light and dark). Accents use `--accent`. Frames use `--line`. Do not hardcode `#fff` or brand hex in consumers.

Spec: `specs/domain/msqdx-ui-inspect-layout-glyphs.md`

## Size modes

<Canvas of={Stories.SizeModes} />

## Media fit

<Canvas of={Stories.MediaFit} />

## Grid columns

<Canvas of={Stories.GridColumns} />

## Grid span

<Canvas of={Stories.GridSpan} />

## All families

<Canvas of={Stories.AllFamilies} />

## Vs Icons

Optical parity with Foundation Lucide icons (`IconColumns`, `IconStretch`, `IconBox`, …).

<Canvas of={Stories.VsIcons} />

## Distribute & cell park

<Canvas of={Stories.DistributeAndPark} />

## Flow / wrap / auto-flow

<Canvas of={Stories.FlowWrapAutoFlow} />

## Position / overflow / border

<Canvas of={Stories.PositionOverflowBorder} />

## Background position & clip presets

<Canvas of={Stories.BgPosAndClip} />

## Self-park & nine-point

<Canvas of={Stories.SelfParkAndNinePoint} />

## Text / aspect / gap

<Canvas of={Stories.TextAspectDisplay} />

## Display modes

Full CSS `display` catalog — consumer wiring optional.

<Canvas of={Stories.DisplayModes} />

## Catalog reserve

Visibility, box-sizing, float, writing-mode, white-space — shipped in DS for later apps.

<Canvas of={Stories.CatalogReserve} />

## Catalog strip

<Canvas of={Stories.Catalog} />

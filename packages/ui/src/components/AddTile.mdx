# AddTile

Layer: `atoms`

Dashed empty-slot control for catalog grids.

**Spec:** `specs/domain/msqdx-ui-add-tile.md`  
**Origin:** Brandion `.brandion-token-add-tile`.

## Stories

- Default / Disabled / Tall

```tsx
import { AddTile } from '@msqdx/ui'

<AddTile label="Add" onClick={() => openEditor()} />
```

# ChatStepList

Layer: `molecules`

Workflow progress rows (`pending` | `running` | `done` | `error`). Spec: `specs/domain/msqdx-ui-chat-step-list.md`.

Compose inside `ChatBlockPanel`. Full tour: **Organisms/ChatCatalog**.

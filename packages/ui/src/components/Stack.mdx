# Stack

Layer: `atoms`

Flex stack for composition layout. Optional `clipInset` applies a corner-inset
`clip-path` via `clipPathFromCornerInsets` (`specs/domain/msqdx-ui-surface-clip.md`).

## Stories

- Default
- BottomSlant — `clipInset.bottomLeft.y = 12%`

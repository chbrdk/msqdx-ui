# HubIndexLayoutSwitch

Layer: `molecules`

Cards | List pill switch for hub index surfaces (projects, compositions, …).

**Spec:** `specs/domain/msqdx-ui-hub-index-layout-switch.md`  
**Origin:** Audion `.audion-editable-comm-layout-switch`

## Stories

- Default (interactive)

```tsx
import { HubIndexLayoutSwitch } from '@msqdx/ui'

<HubIndexLayoutSwitch
  value={layout}
  onChange={setLayout}
  aria-label="Layout"
  cardsLabel="Cards"
  listLabel="List"
/>
```

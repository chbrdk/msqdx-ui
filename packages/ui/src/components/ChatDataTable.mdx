# ChatDataTable

Layer: `molecules`

Compact column/row table for tool results. Spec: `specs/domain/msqdx-ui-chat-data-table.md`.

Compose inside `ChatBlockPanel` (`flush` optional). Full tour: **Organisms/ChatCatalog**.

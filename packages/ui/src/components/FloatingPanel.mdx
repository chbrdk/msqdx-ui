# FloatingPanel

Layer: `organisms`

Dockable workspace overlay (SnapDock). Magazine default: solid / hairline / square.
Opt-in `surface="glass"`. Variants: `panel` | `toolbar` | `strip`.

Spec: `specs/domain/floating-panel.md`

## Tokens consumed

DS-DEPOSIT D5b — FloatingPanel consumes Brandion paths via Layer 0 CSS.

See `knowledge/ds-deposit-component-semantic-map.md`.

| Surface | CSS | Brandion path |
|---------|-----|---------------|
| * | `--paper` / `--line` / `--ink` | `color.surface.base` / `color.line` / `color.ink` |
| glass | `--surface-1` | `color.surface.base` |

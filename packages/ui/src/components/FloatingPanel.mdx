# FloatingPanel

Layer: `organisms`

Dockable workspace overlay (SnapDock). Magazine default: solid / hairline / square.
Opt-in `surface="glass"`. Variants: `panel` | `toolbar` | `strip`.

Spec: `specs/domain/floating-panel.md`

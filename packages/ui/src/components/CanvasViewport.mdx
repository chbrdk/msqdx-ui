# CanvasViewport

Layer: `organisms`

Pan/zoom artboard host. Apps own gesture handlers; pass `panX` / `panY` / `zoom` and optional `overlays` (`SelectionHandles`).

**Spec:** `specs/domain/msqdx-ui-canvas-viewport.md`

## Stories

- Default
- ZoomedAndPanned
- WithSelectionOverlay

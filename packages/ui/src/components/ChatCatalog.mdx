# ChatCatalog

Layer: `organisms`

Discovery tour for chat chrome + message blocks (`ChatBlockPanel`, `ChatBlockList`, turns, composer, overlay).

## Stories

- **Inventory** — static inventory of blocks + compact panel  
- **InOverlay** — same content inside `ChatOverlay`

Deep link: `?path=/story/organisms-chatcatalog--inventory`

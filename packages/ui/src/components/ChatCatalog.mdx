# ChatCatalog

Layer: `organisms`

Discovery tour for chat chrome + message blocks (`ChatBlockPanel`, `ChatBlockList`, `ChatMetricGrid`, `ChatKeyValueList`, `ChatStepList`, turns, composer, overlay).

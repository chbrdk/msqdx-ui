# ComponentPalette

Layer: `organisms`

Clickable and draggable type list. Click calls `onAdd`. Drag sets `COMPONENT_PALETTE_DND_MIME` to the item id so apps can `insert_child` on canvas drop.

See `specs/domain/msqdx-ui-component-palette.md`.

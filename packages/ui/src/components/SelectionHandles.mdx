# SelectionHandles

Layer: `molecules` (Storybook: Organisms / editor chrome)

Visual bounding box + corner affordances. Optional `interactive` + `onHandlePointerDown` for app-owned resize.

**Spec:** `specs/domain/msqdx-ui-selection-handles.md`

## Stories

- Default
- Interactive

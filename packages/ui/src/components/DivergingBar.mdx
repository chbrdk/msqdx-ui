import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './DivergingBar.stories'

<Meta of={Stories} />

# DivergingBar / DivergingBarList

Layer: `Molecules`

Zero-centered signed bars for scorecards (`pos` / `neg` / `neu`).

## Usage

<Canvas of={Stories.Default} />

## Related

Export: `DivergingBarList` from `@msqdx/ui`

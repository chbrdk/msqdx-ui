# Meter / MeterList

Layer: `molecules`

Labeled editable meters built on `Slider` — see `specs/domain/msqdx-ui-meter.md`.

```tsx
import { Meter, MeterList } from '@msqdx/ui'

<MeterList aria-label="Traits">
  <Meter label="Detail" valueLabel="72%" value={72} onChange={…} onCommit={…} />
</MeterList>
```

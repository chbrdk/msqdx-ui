# ScrollArea

Layer: `molecules`

Opt-in hairline scrollport (`.ds-scroll`) — see `specs/domain/msqdx-ui-scroll-area.md`.

Do **not** style `*::-webkit-scrollbar` globally; that forces classic gutters on textareas and every overflow surface.

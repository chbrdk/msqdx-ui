# FlowNodeEditorShell

n8n-like fullscreen node editor: **INPUT** | **Parameters** | **OUTPUT** columns.

Opens over the flow canvas on node selection. Domain fields (`ExpressionField`, `JsonTree`) are supplied by the app via column slots.

See `specs/domain/flow-node-editor-shell.md`.

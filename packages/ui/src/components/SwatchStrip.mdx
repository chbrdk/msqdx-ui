# SwatchStrip

Layer: `atoms`

Horizontal row of CSS color swatches (+ optional caption).

**Spec:** `specs/domain/msqdx-ui-swatch-strip.md`  
**Origin:** Brandion chapter teaser color strip.

```tsx
import { SwatchStrip } from '@msqdx/ui'

<SwatchStrip swatches={['#0B3D2E', '#C4A35A']} label="Brand" />
```

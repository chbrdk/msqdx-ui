# BrandionTokenStudio (composition)

Layer: `templates` (composition docs — not a single export)

Documents Brandion Design Token Studio **chrome** in Storybook:

- `EntityCard` + `AddTile` grid
- `SwatchStrip` chapter teaser
- `Dialog` + `FormSection` + `Field` add/edit body

**Not in `@msqdx/ui`:** Brandion contracts, `TokenTypedFields`, channel metrics, flip animation.

**Specs:** FormSection / EntityCard / AddTile / SwatchStrip under `specs/domain/`.  
**Knowledge:** `knowledge/brandion-ui-promotion.md`

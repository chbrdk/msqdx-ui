import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './MagPage.stories'

<Meta of={Stories} />

# Mag Page

Layer: `Mag`

A4 page chrome for Magazin-PDF. Logo is app-injected — never a hardcoded Coolify URL.

## Docs

<Canvas of={Stories.Docs} />

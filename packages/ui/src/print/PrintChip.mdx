import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './PrintChip.stories'

<Meta of={Stories} />

# Print Chip

Layer: `Print`

Print-Meta-Chips — Twin zu `MagChip`.

**P92:** optional `tone`: `default` | `muted` | `accent` | `solid` (theme accent from Mag/print pack).

## Default

<Canvas of={Stories.Default} />

## Tones

<Canvas of={Stories.Tones} />

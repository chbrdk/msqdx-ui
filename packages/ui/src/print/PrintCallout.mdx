import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './PrintCallout.stories'

<Meta of={Stories} />

# Print Callout

Layer: `Print`

Report wash / emphasis band — Twin zu `MagCallout`. Nicht für attributed Quotes (`PrintPullQuote`).

## Default

<Canvas of={Stories.Default} />

## Variants (P92)

<Canvas of={Stories.Variants} />

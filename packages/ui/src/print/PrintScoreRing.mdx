import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './PrintScoreRing.stories'

<Meta of={Stories} />

# Print ScoreRing

Layer: `Print`

Conic score ring twin of Magazin-PDF `MagScoreRing`.

## Default

<Canvas of={Stories.Default} />

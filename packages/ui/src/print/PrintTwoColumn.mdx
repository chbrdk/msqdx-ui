import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './PrintTwoColumn.stories'

<Meta of={Stories} />

# Print TwoColumn

Layer: `Print`

Magazin-Spread — zwei 50%-Spalten mit Innen-Gutter (Twin zu `MagTwoColumn`).

## Default

<Canvas of={Stories.Default} />

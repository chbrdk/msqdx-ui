import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './PrintChipRow.stories'

<Meta of={Stories} />

# Print Chip Row

Layer: `Print`

Chip-Zeile für Print-Meta. Twin zu `MagChipRow`. CREATION palette type `PrintChipRow`.

## Default

<Canvas of={Stories.Default} />

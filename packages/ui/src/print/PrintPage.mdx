import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as Stories from './PrintPage.stories'

<Meta of={Stories} />

# Print Page

Layer: `Print`

Magazin-Seite mit Folio (Titel + Seitenzahl). Twin zu `MagPage`. CREATION palette type `PrintPage`.

## Default

<Canvas of={Stories.Default} />

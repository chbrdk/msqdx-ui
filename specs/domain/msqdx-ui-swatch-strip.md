# MSQDX UI — SwatchStrip

**Status:** Accepted — 2026-08-10  
**Layer:** Atoms  
**Origin:** Brandion chapter teaser color strip  
**Catalog:** Atoms/SwatchStrip

## Purpose

Horizontal row of color swatches (+ optional caption). Hex / CSS colors only — no token contracts.

## API

`swatches: string[]`, `label?: ReactNode`, `max?: number` (default 8).

## Acceptance

Stories Default / Empty; renders N swatches.
